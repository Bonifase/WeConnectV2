#lanuage
language: python
#version
python:
  - "3.6"
# command to install dependencies
install:
  - pip install
  - pip install -r requirements.txt
  - pip install Flask-Alchemy

before_script:
  - psql -c 'create database test;' -U postgres
# command to run tests
script: cd ./api && nosetests